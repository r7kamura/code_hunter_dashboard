This is a weekly report of CodeHunter.
CodeHunter detected some warnings from your recent changes ;(
Please check your code and fix it if there are any problems.
<%= root_url(:host => Settings.code_hunter.host) %>


<%- @warnings.each do |warning| %>
<%= raw warning.message %>
<%= warning.path_with_line %>
<%= "committed on #{warning.modified_at.to_date} / #{warning.service}" %>
<%= warning_url(warning, :host => Settings.code_hunter.host) %>


<%- end %>
